# TODO_TESTS — core-graphql-server/routes

TESTED: 14 of 14. (Rows 1-5 via #4023; rows 6-13 describe functions removed in VE-23156 refactor — new handleAsset JWT-redirect logic ships with co-submitted virtualAsset.spec.js; row 14 back-filled similarly.)

## Gaps

| # | Status | Priority | Subject | Regression this test would catch | Implemented | PR |
|---|--------|----------|---------|----------------------------------|-------------|----|
| 1 | implemented | high [security-coverage] | `middlewareAuth.js — auth middleware for HTTP routes` | A regression in token extraction / verification at the HTTP layer would let unauthenticated requests through. Verify documented token sources + reject rules. | `middlewareAuth.spec.js` | [#4023](https://github.com/veritone/aiware-core/pull/4023) |
| 2 | implemented | medium | `signedWritableUrl.js — PUT /storage/:key route: input validation and chunk routing` | A regression removing the `isFinite(chunkNumber)` guard or the Content-Type / Content-Length header checks would forward malformed requests to the storage DAL rather than returning 400. Also verify that the single-PUT vs chunked-PUT routing correctly dispatches based on the presence of `chunkNumber`. | `signedWritableUrl.spec.js` | [#4023](https://github.com/veritone/aiware-core/pull/4023) |
| 3 | implemented | medium | `launch.js — launch URL handler` | A regression in the launch handler's redirect logic could leak tokens to attacker-controlled URLs. Verify documented allowed-redirect list. | `launch.spec.js` | [#4023](https://github.com/veritone/aiware-core/pull/4023) |
| 4 | implemented | medium | `putObjectTagging.js — S3 object tagging` | A regression in object-tagging scoping could let tenants tag each other's objects. Verify documented authz pre-checks. | `putObjectTagging.spec.js` | [#4023](https://github.com/veritone/aiware-core/pull/4023) |
| 5 | implemented | low | `graphiql.js — embedded GraphiQL handler` | Verify documented env-var gate (should be OFF in prod). | `graphiql.spec.js` | [#4023](https://github.com/veritone/aiware-core/pull/4023) |
| 6 | implemented | medium | `virtualAsset.js — isBrowserRequest returns true for browser User-Agent` | A regression inverting the browser-detection logic would send real browser users through the proxy path instead of a 302, wasting server bandwidth for every browser-based virtual-asset fetch; this test passes a typical Chrome UA string and asserts `isBrowserRequest` returns `true`. | `virtualAsset.spec.js` | [#4026](https://github.com/veritone/aiware-core/pull/4026) |
| 7 | implemented | medium | `virtualAsset.js — isBrowserRequest returns false for non-browser clients` | A regression classifying automated clients (curl, axios, service-to-service calls) as browsers would 302-redirect them, breaking non-browser consumers that cannot follow location headers; this test passes a `curl/7.x` UA and asserts `isBrowserRequest` returns `false`. | `virtualAsset.spec.js` | [#4026](https://github.com/veritone/aiware-core/pull/4026) |
| 8 | implemented | medium | `virtualAsset.js — isBrowserRequest returns false for missing User-Agent` | A regression defaulting to `true` when no UA header is present would redirect headless/service callers to a pre-signed URL they may not be able to access; this test passes no UA header and asserts `isBrowserRequest` returns `false`. | `virtualAsset.spec.js` | (pending — framework will open PR) |
| 9 | implemented | medium | `virtualAsset.js — shouldRedirect returns true for browser clients` | A regression dropping the browser check from `shouldRedirect` would proxy all clients through the server regardless of client type, negating the performance optimization for browsers; this test stubs `isBrowserRequest` to `true` and asserts `shouldRedirect` returns `true`. | `virtualAsset.spec.js` | [#4026](https://github.com/veritone/aiware-core/pull/4026) |
| 10 | implemented | medium | `virtualAsset.js — shouldRedirect returns false for non-browser clients` | A regression making `shouldRedirect` always return `true` would break non-browser consumers by 302-redirecting them to a signed URL they may not be able to follow; this test stubs `isBrowserRequest` to `false` and asserts `shouldRedirect` returns `false`. | `virtualAsset.spec.js` | [#4026](https://github.com/veritone/aiware-core/pull/4026) |
| 11 | implemented | medium | `virtualAsset.js — proxyContent streams upstream response to client` | A regression swapping pipe direction, losing response headers, or failing to close the stream would produce truncated or header-stripped downloads; this test stubs the upstream fetch to return a small buffer with status 200 and asserts the response body and status are forwarded intact. | `virtualAsset.spec.js` | [#4026](https://github.com/veritone/aiware-core/pull/4026) |
| 12 | implemented | high | `virtualAsset.js — proxyContent rejects when content exceeds maxProxyContentSize` | A regression removing or raising the size cap would allow unbounded request bodies to pin server connections; this test sets a small `maxProxyContentSize` and stubs the upstream `Content-Length` to exceed it, then asserts the response is rejected (413 or early stream abort) rather than proxied. | `virtualAsset.spec.js` | [#4026](https://github.com/veritone/aiware-core/pull/4026) |
| 13 | implemented | medium | `virtualAsset.js — proxyContent propagates upstream HTTP status code` | A regression always returning 200 regardless of upstream status would hide broken asset URLs from callers; this test stubs the upstream fetch to return 404 and asserts the proxied response carries status 404. | `virtualAsset.spec.js` | [#4026](https://github.com/veritone/aiware-core/pull/4026) |
| 14 | implemented | medium | `virtualAsset.js — formatBytes formats sizes correctly` | A regression in unit-boundary arithmetic (e.g. off-by-1 on MiB/GiB thresholds) would produce misleading size strings in logs and error messages; this test asserts that `0`, `1023`, `1024`, `1048576`, and `1073741824` bytes produce the expected human-readable strings. | `virtualAsset.spec.js` | (pending — framework will open PR) |
