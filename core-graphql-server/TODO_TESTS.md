# TODO_TESTS — services/api/core-graphql-server (root)

## Test framework

- **Runner**: Jest (`jest --coverage --coverageDirectory=coverage --maxWorkers=50%`).
- **Test file location**: next-to-source `*.spec.js` (NOT `__tests__/`). Existing precedent: `metering.spec.js`, `monitoring.spec.js`, `coreAdmin.spec.js`, `redisCache.spec.js`, etc.

## Coverage map (depth-1)

TESTED (~24 of ~24): metering, monitoring, util, coreAdmin, rateLimit, customMetrics, redisCache, oktaAuth, initdb, s3Util, coreJob, containerInfo, gqlLogUtil, messageUtil, dbMigrator, core-recording-transcript, fatalErrors, graphqlConfig, buildinfo (VE-24476), localCache (VE-24476), graphqlServer (VE-24480/VE-24537), routes.structuredData (VE-24480), customPlayground (VE-24537), server (VE-24537 — isTraceOn explicit-false case).
PENDING ROWS: 2. Rows 19-20 (filed 2026-08-24) — `s3Util.js`'s new `oci.enabled` credential-resolution catch/else branches (VE-24702/VE-25066), see narrative below. Row 18 (filed 2026-07-22) implemented 2026-07-22 via VE-25777 (util.spec.js — asyncRefreshCacheValue ttlMinOverride timestamp-key TTL match). Rows 12-17 (filed 2026-07-15) implemented 2026-07-18 via VE-25624 (messageUtil.spec.js — buildVtEvent org-id normalization/enrichment, unmatched-event fallback, missing-proto-type guard, legacy dual-emit, produce-failure swallow).
SKIPPED: runtimeCoverage (dev/CI-only tool, no exported production logic), serviceInit (updateDefaultCluster is heavily DB-integrated via dal.cluster — blocked needs-refactor to inject dal stub).
Re-investigated 2026-06-28: server.js changed in VE-23156 added virtualAsset router registration; no new row needed (existing row 1 covers bootstrap verification; virtualAsset logic tested in routes/ subrow).
Re-investigated 2026-07-15 (commit `8da60c89dc`, VE-19358 #4244): `messageUtil.js`'s `buildVtEvent()` (an internal
helper, NOT in the `module.exports` return object — reached only via the exported `emitPublicEvent`) gained a new
org-id `'n/a'`-normalization branch (mirroring the same-PR `core-admin-server/bll/event-util.js` change). Despite
"messageUtil" being marked TESTED above, `messageUtil.spec.js` has **zero** `describe`/`it` blocks that reference
`buildVtEvent` or call `emitPublicEvent` directly — the existing `EmitPublicEvent` describe block (line 166) only
covers the nested `getActionInfo`/`getCallerInfo` helpers, not the emit path itself or `buildVtEvent`'s
control flow (unmatched-event fallback, missing-proto-type guard, org-id enrichment/normalization, or the
try/catch). This is a genuine, substantial gap in an otherwise "TESTED" file — the public surface (`emitPublicEvent`)
and its sole private helper (`buildVtEvent`) are both effectively uncovered. Rows 12-17 below.
Re-investigated 2026-07-22 (commit `a6b6309838`): `util.js`'s `asyncRefreshCacheValue` (returned by `validateCacheKey`,
part of `util.js`'s own public surface — despite "util" being marked TESTED above) gained a new second parameter
`ttlMinOverride` (T28, `fix/t28-rbac-negative-cache-ttl`). `util.spec.js`'s existing `asyncRefreshCacheValue` tests
only exercise the single-arg call; none pass `ttlMinOverride` or assert the timestamp key receives the same TTL as
the value key. The RBAC caller (`authACE.dal.js`/`authACE.dal.spec.js`) covers the *integration* of this branch
through `hasPermissionsWithCache`, but `util.js`'s own direct unit surface remains gapped. Row 18 below.
Re-investigated 2026-08-24 (commit `71864b9dc8`): diff-scoped from `a6b6309838` (2026-07-22); confirmed it is an
ancestor of HEAD (`git merge-base --is-ancestor a6b6309838 71864b9dc8` passed) so `git log a6b6309838..71864b9dc8 --
services/api/core-graphql-server` (depth-1 only) was used directly - 7 commits, 6 depth-1 production files touched.
`customMetrics.js`/`monitoring.js` (VE-25603): new `analyzeMonTables`/`analyzeMonErrors` Prometheus counters plus a
heartbeat-stats mapping entry - purely declarative; `monitoring.spec.js`'s existing stats-count assertion was bumped
24 to 26 in the same commit, no gap. `server.js` (VE-17601): a 9-line loop forwarding
`server.headers.csp.sanitizationWarnings` to `logger.warn` at startup - the actual CSP-sanitization logic lives in
the new `util/contentSecurityPolicyUtil.js` (own exhaustive spec) and `config/config.js` (own spec + that
directory's own TODO_TESTS.md, which already files the `sanitizeCspConfig` catch-arm gap); this loop itself is part
of `setupServer()`'s bootstrap, already flagged as a mocking-complexity-deferred gap in row 1 - no new row filed.
`initdb.js` (db-credential-leakage redaction): new `redactUri`/`redactText` helpers plus a malformed-connection-
string guard in `setupConnection`, exhaustively covered by 13 new `initdb.spec.js` cases (both `redactUri`/
`redactText` branches, plus the malformed-URI throw path with a plaintext-password assertion) - no gap.
`messageUtil.js` (`emitErrorEvent`): new try/catch around `JSON.stringify(error.data)` guarding against circular
structures (TLS cert chains) - covered by a new `messageUtil.spec.js` case that forces a circular `data.error.cert`
and asserts the `dataUnserializable` fallback reaches the logger - no gap. `s3Util.js` (VE-24702/VE-25066, OCI
storage credential resolution): new `resolveExternalCredential`/`getOciSecret` helpers and an `oci.enabled`
credential-resolution block (lines ~262-292) that overrides the top-level S3 credentials when OCI is the active
backend. The two new `s3Util.spec.js` tests cover only the happy paths (top-level `s3.accessCredentialId` resolves;
`oci.accessCredentialId` resolves and overrides top-level credentials) - the block's own `try/catch` has zero
coverage on either failure side: neither the `catch` (decrypt/DB failure resolving OCI credentials, logs via
`logger.error`, falls back to whatever `getAWSSecret()` already returned) nor the `else` (credentials resolve to
null/incomplete, logs via `logger.warn`, same fallback) is exercised by any test. Rows 19-20 below.

## Gaps

| # | Status | Priority | Subject | Regression this test would catch | Implemented | PR |
|---|--------|----------|---------|----------------------------------|-------------|----|
| 1 | implemented | medium | `server.js — Apollo server bootstrap` | A regression in the Apollo bootstrap (e.g. failing to register a documented context-builder or plugin) would break authn at the resolver level. Verify the documented plugins + context-builders are registered. | `graphqlServer.spec.js` (isTraceOn explicit-false case added; full plugin/context-builder bootstrap deferred — setupServer() mocking complexity out of scope for this batch) | VE-24537 |
| 2 | done | medium | `metrics.js — prom counter setup` | A regression that drops a documented metric (graphql_operations_total, errors_total, etc.) would silently break dashboards. Verify the exported metrics object contains the documented names. | `metrics.spec.js` (7 cases: shape, check-throws-on-unknown-metric, inc/observe/resetGauge delegation, getValue sum + label match) | VE-23622 |
| 3 | implemented | low | `customPlayground.js — playground enable/disable` | A regression that enables the GraphQL playground in production would expose schema introspection. Verify the documented env-var gate. | `customPlayground.spec.js` (10 tests: applyMiddleware nil-guards x4, withBanner nil/empty guards x3, withBanner handler registration + HTML/non-HTML behaviour x3) | VE-24537 |
| 4 | implemented | high [security-coverage] | `dbMigrator.js:213 — migrateDB (invalid dbName → throws before Flyway exec)` | A regression removing the `[A-Za-z0-9_-]` guard on `dbName` would allow shell-metacharacter injection into the Flyway config file path, bypassing the `execFileSync` OS-command-injection defense. Verify that a `dbName` containing `/`, `;`, `$()`, or spaces throws `"Invalid dbName '...'"`. | VE-23514 | TBD |
| 5 | implemented | high [security-coverage] | `dbMigrator.js — migrateDB (configPath path traversal → throws)` | A regression removing the CWD bounds check on `configPath` (VE-23309) would allow a path like `../../../etc` to resolve outside the service directory, enabling path traversal. Verify `path.resolve(configPath)` outside `path.resolve('.')` throws `"must be within the service directory"`. | VE-24274 | TBD |
| 6 | implemented | medium | `localCache.js:22 — getCache (LRU TTL in ms from config-driven minutes)` | A regression removing the `* 60 * 1000` multiplier would set TTL in minutes instead of milliseconds, causing entries to expire 60,000× sooner and flooding every downstream service with cache misses. Verify that `config.localCache.ttlMin = 5` produces an LRUCache with TTL of `300000` ms. | `localCache.spec.js` | VE-24476 |
| 7 | implemented | medium | `localCache.js:62 — clearBy (deletes only entries satisfying condition)` | A regression that clears all entries regardless of the conditional check would evict unrelated cached objects on every targeted invalidation call, causing cache stampedes across unrelated object types. Verify that only entries passing the predicate are deleted and others survive. | `localCache.spec.js` | VE-24476 |
| 8 | implemented | low | `buildinfo.js — getBuildInfo (lazy singleton; returns 'unknown' fields on missing JSON)` | A regression removing the singleton guard would re-parse buildinfo.json on every call; a regression in the fallback branch would surface a raw exception instead of the safe `'unknown'` values on systems without the file. Verify the fallback shape and that a second call returns the cached object. | `buildinfo.spec.js` | VE-24476 |
| 9 | implemented | low | `graphqlServer.js — GraphqlServer constructor (traceOn defaults to false when config key absent)` | A regression that reads a different config key or removes the `hasOwnProperty` check would either enable tracing in production (performance/data-exposure risk) or fail to enable it in test environments. Verify that `new GraphqlServer({config: {}}).isTraceOn()` returns `false` and that `new GraphqlServer({config: {enableGraphQLTracing: true}}).isTraceOn()` returns `true`. | `graphqlServer.spec.js` | VE-24480 |
| 10 | implemented | medium | `graphqlServer.js — setupSubscriptionServer() skips SubscriptionServer creation when enableSubscription=false` | A regression removing the `graphqlConfig.enableSubscription` gate would create a WebSocket subscription server on every instance, opening an unauthenticated WS upgrade endpoint. Verify that when `enableSubscription=false`, no `SubscriptionServer` is constructed (spy on `SubscriptionServer` constructor). | `graphqlServer.spec.js` | VE-24480 |
| 11 | implemented | medium [security-coverage] | `routes.structuredData.js — preAuthParseQueryJwt extracts token from query string` | A regression that skips the query-string JWT extraction would cause structured-data POST requests with Bearer tokens in the query string (the documented auth method for SDO ingest) to fail authentication. Verify that a request with `?Authorization=Bearer+<token>` has the Authorization header set before the auth middleware runs. | `routes.structuredData.spec.js` | VE-24480 |
| 12 | implemented | high | `messageUtil.js:291 — buildVtEvent` (NEW: org-id `'n/a'` normalization branch) | A regression that removes or narrows the new normalization branch (`core.organizationId === 'N/A'` no longer folding to `'n/a'`) would leave the legacy uppercase `'N/A'` sentinel in audit events emitted from GraphQL mutations, breaking downstream consumers (e.g. `core-admin-server`'s `audit-log.js`) that filter/group by the lowercase `'n/a'` sentinel. `messageUtil.spec.js` has zero coverage of `buildVtEvent` at all. | `messageUtil.spec.js` | VE-25624 |
| 13 | implemented | medium | `messageUtil.js:291 — buildVtEvent` (caller-org enrichment preserved when a real org id is present) | A regression where the new normalization branch fires before/instead-of the caller-org enrichment (or overwrites an already-enriched real org id) would strip a legitimate organization id from every audit event and replace it with `'n/a'`, losing tenant attribution across the board. | `messageUtil.spec.js` | VE-25624 |
| 14 | implemented | medium | `messageUtil.js:291 — buildVtEvent` (unmatched `eventName` falls back to `eventsMap.Unknown` + logs warning) | A regression that removes the `eventsMap.Unknown` fallback (or the `logger.warn` call) for an unrecognized `eventName` would either throw deep inside `_buildCore`/`events[event.name]` or silently drop the event with no diagnostic trail. | `messageUtil.spec.js` | VE-25624 |
| 15 | implemented | medium | `messageUtil.js:291 — buildVtEvent` (missing proto event type → logs warning, returns `null`) | A regression that removes the `_.isNil(eventDataType)` guard would throw when a proto message type isn't registered under `events[event.name]`, instead of degrading gracefully (`logger.warn` + `return null`) — would crash the calling `emitPublicEvent`/mutation resolver instead. | `messageUtil.spec.js` | VE-25624 |
| 16 | implemented | medium | `messageUtil.js:252 — emitPublicEvent` (legacy dual-emit via `eventNamesChange`) | A regression that breaks the recursive re-emit for a legacy-renamed event (`eventNamesChange[eventName]` + `eventsMap[newEventName]` both truthy) would stop double-publishing under both the old and new event names, breaking any downstream consumer still subscribed to the legacy name. | `messageUtil.spec.js` | VE-25624 |
| 17 | implemented | medium | `messageUtil.js:252 — emitPublicEvent` (`messaging.produce` failure is caught and swallowed, not rethrown) | A regression that lets a `messaging.produce` rejection propagate instead of being caught (incrementing the `messageFailed` counter and returning `undefined`) would turn a downstream NSQ outage into an unhandled promise rejection for every calling mutation resolver, instead of the documented fire-and-forget degrade. | `messageUtil.spec.js` | VE-25624 |
| 18 | implemented | medium | `util.js:1594 — asyncRefreshCacheValue` (NEW 2nd param `ttlMinOverride`; timestamp key must share the value key's override TTL) | T28 (`fix/t28-rbac-negative-cache-ttl`) added a per-call `ttlMinOverride` so a caller (e.g. RBAC's negative-cache path) can shorten the L2 TTL below the closure's default `ttlMinL2Override`. `util.spec.js`'s existing `asyncRefreshCacheValue` tests only call it with a single arg (no override) — none exercise `ttlMinOverride` directly, and none assert that the **timestamp** key's `redisCache.asyncSet` call also receives the override TTL (only the value key was previously known to take a TTL arg). A regression that applies the override to the value key but not the timestamp key would let the timestamp outlive the value, silently reintroducing the T28 stale-denial bug this fix was written to close — `isCacheDirty()`'s self-heal compares against a timestamp that never expires while the value already has. Covered only indirectly today, through `authACE.dal.spec.js`'s two new TTL-branch tests, which exercise `hasPermissionsWithCache` (a caller) rather than `asyncRefreshCacheValue` itself. | `util.spec.js` | VE-25777 (pending — PR opening) |
| 19 | implemented | medium | `s3Util.js:284 — oci.enabled credential-resolution catch arm (getOciSecret() throws)` | A regression that lets this catch propagate (crashing server startup on any decrypt/DB failure while resolving `oci.accessCredentialId`) or that discards the error without logging would either take down startup on a transient `sso` DB blip, or hide an OCI-credential misconfiguration from operators. Verify that when `getOciSecret()` rejects (e.g. `decryptObject` throws on a malformed/mismatched-key ciphertext), `serviceContext.logger.error` is called, module load does not throw, and `credentials` remains whatever `getAWSSecret()` had already resolved. | `s3Util.spec.js` ("should log an error and keep the previously-resolved credentials when oci.enabled and getOciSecret() throws (VE-27835 #19)") | VE-27835 (pending — PR opening) |
| 20 | implemented | medium | `s3Util.js:272 — oci.enabled credential-resolution else arm (getOciSecret() resolves null/incomplete)` | A regression that removes the `accessKey && secretKey` completeness check (or the `logger.warn` call) would silently proceed to sign OCI URLs with null/partial credentials instead of surfacing the misconfiguration, or would overwrite valid existing `credentials` with an incomplete object. Verify that when `oci.enabled=true` and neither inline `oci.accessKey`/`oci.secretKey` nor a resolvable `oci.accessCredentialId` are configured, `serviceContext.logger.warn` fires and `credentials` is left as whatever `getAWSSecret()` returned (not nulled or partially overwritten). | `s3Util.spec.js` ("should warn and keep the previously-resolved credentials when oci.enabled but no usable OCI credentials resolve (VE-27835 #20)") | VE-27835 (pending — PR opening) |
