# TODO_TESTS — services/api/core-graphql-server/dal

Curated by `starlord-aqa` (automation; formerly the `worker-psd-aqa` VM agent). Each row is
one proposed test addition with a clear regression statement. Anyone (human or automation) may add
rows; only the automation updates `Implemented` and `PR`.

Append-only at the bullet level — implementation marks a row done, it never disappears.

## Test framework

- **Runner**: Jest. Tests live NEXT-TO-SOURCE as `<name>.spec.js`.
- **Mocking pattern**: hand-rolled fake DB connections (`coreDbRead._push([...])` etc.) and a
  fake `serviceContext.messageUtil` with `_messages()`/`_clearCounter()` helpers — not
  `jest.mock`-based module mocking.

## Coverage map (depth-1)

TESTED: applicationViewer, asset, organization, and many more (large coverage surface — tests are co-located).

## Gaps

| # | Status | Priority | Subject | Regression this test would catch | Implemented | PR |
|---|--------|----------|---------|----------------------------------|-------------|----|
| 12 | implemented | medium | `dalFlowRevision.js — getFlowRevisionsByIds` (VE-25953) | The commit message documents image-version substitution on three read paths — `getFlowRevision`, `getRevisionsDb`, and `getFlowRevisionsByIds` — but `dalFlowRevision.spec.js`'s new `#applyCurrentImageVersions` suite only exercises the first two. A regression that dropped the `applyCurrentImageVersionsToRecords` call from `getFlowRevisionsByIds` specifically would go uncaught. | `dalFlowRevision.spec.js` — "should substitute versions for every record on the getFlowRevisionsByIds read path with one lookup (VE-27857 row 12)" | VE-27857 |
| 13 | pending | medium | `dalFlowRevision.js — getCurrentNodeRedImageVersions`, `getControllerNodeRedUrl` failure branch (VE-25953) | The outer `try { controllerUrl = await getControllerNodeRedUrl(...) } catch { logger.warn(...); return null; }` guard is distinct from the fetch-failure paths already tested (which mock `global.fetch`, not the URL-resolution step). A regression here would let a `getControllerNodeRedUrl` rejection propagate into the flow-revision read path instead of degrading to the stored runtime version. | | |
| 14 | implemented | medium | `dalFlowRevision.js — nodeRedImageVersionCache` negative-cache TTL (`IMAGE_VERSION_CACHE_ERROR_TTL_MS`, VE-25953) | The existing cache test (`should reuse a cached lookup within the TTL and refresh after it expires`) only exercises the 60s success-TTL branch. The 10s failure-TTL branch (`value ? IMAGE_VERSION_CACHE_TTL_MS : IMAGE_VERSION_CACHE_ERROR_TTL_MS`) is unverified — a regression collapsing both TTLs, or dropping negative caching entirely (re-probing a down controller on every request), would not be caught. | `dalFlowRevision.spec.js` — "should use the shorter error TTL (not the success TTL) when the lookup fails, refreshing sooner (VE-27857 row 14)" | VE-27857 |
| 15 | pending | low | `dalFlowRevision.js — redactUrl` catch branch (VE-25953) | `redactUrl` returns `'<unparseable-url>'` when `new URL(url)` throws; only the well-formed-URL-with-credentials case is tested. A regression that let an unparseable controller URL throw uncaught (instead of degrading the log line) would go uncaught. | | |
| 16 | pending | low | `dalFlowRevision.js — imageRepository` registry-port vs. tag disambiguation (VE-25953) | `lastColon > lastSlash` distinguishes a registry port (e.g. `host:5000/repo`) from a tag (`repo:tag`); only plain `registry.central.aiware.com/node-red-v3:tag`-shaped references are exercised by the validation tests. A regression flipping this comparison would misclassify a port-qualified registry's repository, silently breaking the allow-list match for any environment using one. | | |
| 17 | implemented | medium | `asset.js — emitAssetAuditEvent` inner `emitPublicEvent` failure (VE-15782) | The `try { messageUtil.emitPublicEvent(...) } catch (err) { logger.error(...) }` inside `emitAssetAuditEvent` is untested — every audit-event test lets `emitPublicEvent` succeed. A regression dropping this inner catch would let a message-bus hiccup during audit publishing propagate out of `updateAsset`/`deleteAsset` (or, inside the failure-path call, mask the original asset error), even though the underlying DB operation already succeeded/failed independently of the audit event. | `asset.spec.js` — "does not let a message-bus failure on the audit publish propagate out of updateAsset (VE-27857 row 17)" | VE-27857 |
| 18 | pending | low | `tdo.js — buildPreviousTimes` nil-guard branch (VE-26223) | `buildPreviousTimes` returns `undefined` when `previousTdo`/its start-stop times, or the new start/stop times, are nil. The three new tests (trimmed / not-shorter / preventTrim) all supply well-formed timestamps on both sides; the defensive nil-guard branch itself is unexercised — a regression there (e.g. throwing on a TDO record with a missing `stopDateTime`) would surface as an unhandled rejection on `updateTDO` rather than a clean skip of stale-slice cleanup. | | |
| 3 | implemented | high [security-coverage] | `jwt.js — JWT issuance / verification` | A regression in JWT verification (e.g. accepting `alg: none`, ignoring `iss`/`aud` mismatch) would re-introduce a class of auth-bypass vulnerabilities. Verify documented verify rules + reject-conditions. | `jwt.spec.js` |  |
| 4 | implemented | medium | `dalFolder.js — MAX_DEPTH reads from config with fallback` | A regression hardcoding `MAX_DEPTH = 5` again (bypassing the config read) would prevent operators from tuning folder depth limits via config, potentially breaking deployments that set `folder.maxDepth` to a non-default value; this test stubs the config module to return a custom `folder.maxDepth` value and asserts `MAX_DEPTH` (or the depth-check behavior) reflects that value rather than the hardcoded `5`. | `dalFolder.spec.js` — "dalFolder — MAX_DEPTH is config-driven (folder.maxDepth)" | VE-24737 / [#4379](https://github.com/veritone/aiware-core/pull/4379) |
| 2 | implemented | medium | `jsonSchema.util.js — JSON Schema utility helpers` | A regression in the schema-traversal helpers would silently corrupt schema-driven code generation. Verify the documented helpers' happy paths. | `jsonSchema.util.spec.js` |  |
| 1 | implemented | medium | `dalFlowExecution.js — flow execution persistence` | A regression in the flow-execution DAL (state transitions, partition routing) would surface as flow-runs being saved to the wrong shard. Verify the documented SQL shape + the state-transition table. | `dalFlowExecution.spec.js` |  |
| 5 | implemented | high [security-coverage] | `asset.js:createAssetAuthorized — resolverUtil.stripOwnedStorageUrlSignature called on URI before DB write` | A regression removing `input.uri = resolverUtil.stripOwnedStorageUrlSignature(input.uri)` from `createAssetAuthorized` would persist a signed URL (with SAS/presigned params) into the DB instead of a canonical URI. Any consumer reading that URI and adding new credentials would produce a double-signed URL or expose embedded credentials in stored data. | `asset.spec.js` ("should strip query parameters from our signed asset URI before persisting") | [#4034](https://github.com/veritone/aiware-core/pull/4034) |
| 6 | implemented | high [security-coverage] | `asset.js:updateAssetUri — resolverUtil.stripOwnedStorageUrlSignature called before SQL update` | A regression removing the `stripOwnedStorageUrlSignature` call from `updateAssetUri` would write signed URLs into the `asset_uri` column, embedding expiring credentials in the database. | `asset.spec.js` ("should strip query parameters from our signed asset URI before updating") | [#4034](https://github.com/veritone/aiware-core/pull/4034) |
| 7 | implemented | high [security-coverage] | `asset.js:updateExistingAccumulatedResult — resolverUtil.stripOwnedStorageUrlSignature called before UPDATE SET uri` | A regression removing `normalizedUri = resolverUtil.stripOwnedStorageUrlSignature(input.uri)` from `updateExistingAccumulatedResult` would store a signed URL in the `uri` column when an engine re-uploads an accumulated result to our own bucket. Existing `uploadEngineResult` tests use plain URIs and do not exercise the strip path. Test: call `uploadEngineResult` with `isAccumulatedResult: true` and a signed S3 URI belonging to our bucket; assert the `UPDATE` SQL receives the canonical (stripped) URI rather than the raw signed one. | `asset.spec.js` ("strips the signed-URL signature before UPDATE…") | VE-24553 |
| 8 | implemented | low | `dalEngine.js:~460 — NotFound error objectId falls back to _.first(args.ids) when args.id is absent` | A regression reverting this to `objectId: args.id` (without the `|| _.first(args.ids)` fallback) would produce a `NotFound` error with `objectId: undefined` when `getEngines` is called with an `ids` array rather than a single `id`. Verify that when `args.id` is `undefined` and `args.ids = ['eng-1']`, the thrown `NotFound` carries `objectId: 'eng-1'`. | `dalEngine.spec.js` — `should throw NotFound with objectId from args.id when engine not found` | (pending — framework will open PR) |
| 9 | implemented | high [security-coverage] | `event.js:186-188 — emitEvent CWE-73 path-traversal guard (eventType leg)` | VE-24739/#4302 added a guard rejecting `application`/`eventType` values containing a path separator or a bare `..`; the merged commit's own new test (`event.spec.js` "should reject path traversal in application that escapes definitionPath") only exercises the `application` argument (`'../escape'`). The symmetric `eventType` leg of the same guard (`/[/\\]/.test(type) \|\| type === '..'`) has zero test coverage — a regression that dropped only the `type` half of the check (e.g. an incomplete refactor) would not be caught by any existing test. | `event.spec.js` ("should reject path traversal in eventType that escapes definitionPath (VE-25591)") | (pending — framework will open PR) |
| 10 | implemented | medium | `organizationInvite.js:420-424 — createOrganizationInvite (T55: _recordAction now awaited)` | PR #4473 (fix/t55-orginvite-replica-lag) changed the creation-audit `_recordAction(...)` call from fire-and-forget to `await _recordAction(...)`, so a failed audit-row insert now propagates and rolls back the invite transaction instead of silently committing the invite with no audit row. None of `#createOrganizationInvite`'s existing specs mock `_recordAction` to reject — a regression that dropped the `await` again (reverting to the pre-fix silent-failure behavior) would pass every current test. Needs a test that stubs the tx/query layer so the `_recordAction` insert rejects, and asserts `createOrganizationInvite` rejects too (transaction not left in a partially-committed state). | `organizationInvite.spec.js` ("T55: should propagate rejection when the creation-audit insert fails, instead of resolving with the invite") | VE-25903 (pending — PR opening) |
| 11 | implemented | high [security-coverage] | `dalFolderV2.js:1740 -- _fixupV1UserRootFolder` subtree-scoped org reassignment (VE-26538) | #4569 fixed a WHERE-clause operator-precedence bug: the prior `organization_id = -1 OR organization_id IS NULL AND folder_path <@ (...)` parsed as `(organization_id = -1) OR (organization_id IS NULL AND folder_path <@ ...)` (AND binds tighter than OR), so once ANY -1-org folder existed, the unscoped `organization_id = -1` branch matched and re-tenanted every -1 folder/object across ALL organizations to the resolving user's org -- a cross-tenant data-isolation bug. The fix scopes both the folder and filed-object UPDATEs to the resolved root's `folder_path <@ (...)` subtree via a CTE. The one existing test exercising this function (`dal.getRootFolders.spec.js` "should handle V1 user root folder fallback when org is oldest") only asserts the overall `getRootFolders` call resolves and returns folders -- it never inspects the SQL text or bound params passed to the second `dbWrite.query`/`write._push` mock call, so it would pass identically whether the WHERE clause is correctly subtree-scoped or globally unscoped (the pre-fix bug). Needs a test asserting the query passed to the mocked write connection is parameterized by `folderId` (not global) -- e.g. two sibling root-folder subtrees with -1-org folders, only the resolving root's subtree should flip to the new org. Also uncovered: the `if (result)` false branch (root org not yet resolved -- the `_fixupV1UserRootFolder`'s own UPDATE returns no row) never reaches the subtree-fixup query at all; a regression accidentally reordering these could run the fixup query unconditionally. | `dalFolderV2.spec.js` ("should scope the V1 root-folder org reassignment to the resolving root's own subtree, not globally (VE-26538)") | (pending — framework will open PR) |
