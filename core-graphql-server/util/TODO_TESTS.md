# TODO_TESTS — core-graphql-server/util

Curated by `worker-psd-aqa` / `starlord-aqa` (automation). Each row is one proposed test addition
with a clear regression statement. Anyone (human or this agent) may add rows; only the agent
updates the `Implemented` and `PR` columns.

Append-only at the bullet level: once a row exists, don't silently delete it. Implementation marks the row done.

Source files (depth-1): 15 files now (was 12 as of the VE-23156 note below — `contentSecurityPolicyUtil.js`
VE-17601, `ttlExistenceCache.js` / `redisExistenceL2.js` VE-24702 added since). Test framework: Jest 29.7.0,
next-to-source `*.spec.js`.
NOTE: `jest.mock('fs', ...)` must appear before `require('fs')` (no Babel transform → no auto-hoisting).
TESTED (12/12 as of VE-23156): storageEndpoint.spec.js added alongside storageEndpoint.js; all 12 files as of
that note were covered. `contentSecurityPolicyUtil.js`, `ttlExistenceCache.js`, `redisExistenceL2.js` each
shipped with thorough co-authored specs (re-verified 2026-08-20 — see rows 16-17 for the only residual gaps
found); `presigner.s3.buckets.js`'s VE-24702 credential/fallback-grouping addition is likewise fully covered
by its paired spec.

| # | Status | Priority | Subject (file:line — function) | Regression this test would catch | Implemented | PR |
|---|--------|----------|--------------------------------|----------------------------------|-------------|----|
| 1 | implemented | medium | `db.js:2 — dbWriteTx (no openTx: acquires a real write connection and returns operational tx object)` | A regression in the no-openTx branch (wrong connection pool key, missing begin/commit/rollback/done) would surface as unhandled promise rejections across every DAL caller. | db.spec.js | [#3923](https://github.com/veritone/aiware-core/pull/3923) |
| 2 | implemented | medium | `db.js:2 — dbWriteTx (openTx provided: reuses client and returns no-op begin/commit/rollback/done)` | A regression that causes begin/commit to be non-noop when openTx is provided would double-commit or double-rollback a caller-managed transaction. | db.spec.js | [#3923](https://github.com/veritone/aiware-core/pull/3923) |
| 3 | implemented | medium | `elastic.js:48 — authenticate (both username+password → adds Authorization header)` | A regression that removes or corrupts the Basic auth header construction would silently send unauthenticated ES queries. | elastic.spec.js | [#3923](https://github.com/veritone/aiware-core/pull/3923) |
| 4 | implemented | medium | `elastic.js:48 — authenticate (only username OR only password → throws)` | A regression that drops the partial-credential guard would allow a misconfigured service to start without error, then fail on every ES request with a confusing auth error. | elastic.spec.js | [#3923](https://github.com/veritone/aiware-core/pull/3923) |
| 5 | implemented | medium | `elastic.js:56 — newConnection (returns new client with authenticated headers)` | A regression in newConnection that passes unauthenticated headers to the returned client would silently break any non-default ES connection. | elastic.spec.js | [#3923](https://github.com/veritone/aiware-core/pull/3923) |
| 6 | implemented | medium | `expressUtil.js:8 — cleanupMultipartUploads (req.file present: file is unlinked on response finish)` | A regression that drops the req.file cleanup would leave temporary upload files on disk, causing disk-exhaustion on busy instances. | expressUtil.spec.js | [#3923](https://github.com/veritone/aiware-core/pull/3923) |
| 7 | implemented | medium | `expressUtil.js:8 — cleanupMultipartUploads (req.files as array: all files unlinked on response finish)` | A regression in the Array.isArray branch that skips cleanup for array-form req.files would leave multiple temp files on disk per multipart upload. | expressUtil.spec.js | [#3923](https://github.com/veritone/aiware-core/pull/3923) |
| 8 | implemented | medium | `expressUtil.js:8 — cleanupMultipartUploads (req.files as object map: all files from all field keys unlinked)` | A regression in the object-form iteration that misses some field keys would leave orphaned temp files for multi-field multipart uploads. | expressUtil.spec.js | [#3923](https://github.com/veritone/aiware-core/pull/3923) |
| 9 | implemented | medium | `presigner.s3.buckets.js:349 — buildBucketConfigMap()` (per-bucket MinIO endpoint override) | A regression where `bucket.minioEndpoint` is ignored (always uses global `minioConf.endPoint`) would generate presigned MinIO URLs pointing to the wrong endpoint for buckets with a dedicated MinIO instance, silently routing signed-URL traffic to the wrong server. Verify `bucket.minioEndpoint` takes precedence over `minioConf.endPoint` in the returned config. | presigner.s3.buckets.spec.js | [VE-23219] |
| 10 | implemented | high | `presigner.s3.buckets.js:627 — getMinioPresigner(bucketConf)` (Map cache keyed by bucket name) | A regression where the cache key is always `'global'` (or a constant) would share one presigner instance across all buckets, using the first bucket's credentials for all subsequent MinIO signing — silently authorizing URLs for bucket B using bucket A's secret key. Verify that two distinct `bucketConf.name` values produce two independent cached presigners. | presigner.s3.buckets.spec.js | [VE-23219] |
| 11 | implemented | high | `presigner.s3.buckets.js:627 — getMinioPresigner(bucketConf)` (bucket-level credentials override) | A regression where `bucketConf.accessKey/secretKey` are ignored (always falls back to global `minioConf.accessKey/secretKey`) would cause all MinIO URL signing to use global credentials even when per-bucket credentials are configured, potentially granting cross-bucket access. Verify that a `bucketConf` with explicit `accessKey` creates a presigner using those keys. | presigner.s3.buckets.spec.js | [VE-23219] |
| 12 | implemented | medium | `presigner.s3.buckets.js:366 — buildFallbackMap()` (MinIO field propagation from primary to fallback) | A regression that omits MinIO field propagation from primary `bucketConf` to the `fallbackConfig` would cause fallback bucket URL signing to use `null` endpoint/port, generating malformed presigned URLs. Verify that when the fallback bucket doesn't set its own `minioEndpoint`, the primary's value is inherited in `fallbackConfig`. | `presigner.s3.buckets.spec.js` — `should propagate name and MinIO fields to fallbackConfig` | (in existing file, prior to AQA) |
| 13 | implemented | medium | `storageEndpoint.js:25 — isLocalStorageEndpointEnabled returns true when featureFlags.signedWritableUrlOverride is true` | A regression requiring both flags (AND instead of OR) would prevent the storage upload route from registering for services using only `signedWritableUrlOverride`, breaking PUT /storage uploads for those deployments. | `storageEndpoint.spec.js` | VE-23156 |
| 14 | implemented | medium | `storageEndpoint.js:25 — isLocalStorageEndpointEnabled returns true when oci.enabled is true` | A regression requiring both flags would prevent the upload route from mounting for OCI-only deployments, silently breaking all OCI asset uploads. | `storageEndpoint.spec.js` | VE-23156 |
| 15 | implemented | low | `storageEndpoint.js:25 — isLocalStorageEndpointEnabled returns false when neither flag is set` | A regression defaulting to `true` would register the raw-body bypass and upload route unconditionally, exposing the internal PUT /storage endpoint to all environments regardless of OCI/signedWritableUrl configuration. | `storageEndpoint.spec.js` | VE-23156 |
| 16 | implemented | medium | `elastic.js — buildConnectionOptions(targetHost)` per-connection host wiring | `buildConnectionOptions` is called with the outer default `host` for the client singleton and with `newConnection`'s own (shadowed) `host` parameter for each new connection, so `createInstrumentedAgent` gets the right per-connection host. The new pooling tests only assert `agent.options.{keepAlive,maxSockets,maxFreeSockets}` (host-independent config); no test asserts `createInstrumentedAgent` actually receives the connection's own host rather than always reusing the default client's host. A regression hardcoding the outer `host` inside `newConnection`'s call would silently mis-pool/mis-label every non-default ES connection — exactly the class of bug this fix (#4416, connection leaks) was written to prevent — with all four existing pooling tests still green. | elastic.spec.js | [#4666](https://github.com/veritone/aiware-core/pull/4666) |
| 17 | implemented | low | `contentSecurityPolicyUtil.js:50 — sanitizePolicy`, directive-name case sensitivity | `SOURCE_LIST_DIRECTIVES.has(name.toLowerCase())` is the only thing making directive matching case-insensitive; every existing test uses an already-lowercase directive name (`script-src`, `img-src`, etc.). A regression dropping the `.toLowerCase()` would silently stop sanitizing any mixed-/upper-case source-list directive (e.g. `Script-Src`), letting a multi-port SSRF-shaped source through unsanitized for that spelling. | contentSecurityPolicyUtil.spec.js | [#4666](https://github.com/veritone/aiware-core/pull/4666) |
