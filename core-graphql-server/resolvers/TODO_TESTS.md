# TODO_TESTS — core-graphql-server/resolvers

**81 source files. Substantial gap pool (~39 untested).** This is the top-level resolver layer for the GraphQL API.

## High-priority subset

Per-file enumeration of all 39+ gaps is deferred. Below are the high-value gap clusters by domain.

| # | Status | Priority | Subject | Regression this test would catch | Implemented | PR |
|---|--------|----------|---------|----------------------------------|-------------|----|
| 21 | pending | high [security-coverage] | `RegistrationConfigurationInfo.js:14 — files` (public `@noAuth` resolver, VE-27069) | The `files` resolver has **zero** test coverage — not the shape mapping (`fileId` aliasing, `_.camelCase(type)`, `util.getSignedUrlOrVirtual(url)`) nor the newly-added `status: 'active'` filter that now excludes superseded registration documents from this public no-auth view (the admin-facing `RegistrationConfiguration` counterpart deliberately does not filter). A regression dropping that filter, or reverting to the unfiltered admin query, would silently re-expose superseded registration documents on the public registration page and to the first-login terms check. | | |
| 18 | pending | medium | `util.js:303 — getVirtualSignedUri` new `appendFilenameSegment` integration (VE-26469) | `getVirtualSignedUri` now pipes `{fileName, assetId: id, contentType, uri: baseUri}` through the shared `@veritone/core-server-base/assetFilename` derivation before returning the token URI. The function itself has zero direct unit tests — `Asset.spec.js` mocks `util.getVirtualSignedUri` out entirely rather than exercising the real implementation — so a wrong param name, a dropped field, or a shape change in the shared derivation module would go undetected; a thrown error here is swallowed by the outer try/catch and surfaces as the generic "Failed to create virtual signed uri" 500 for every virtual-asset URL request, not just this one field. |  |  |
| 19 | pending | medium | `AssetSourceData.js:60 — schema` resolver, cache-miss branch (VE-24648) | The id+dataRegistryId fast path calls `dal.structuredData.getSchemaRowFromCache(obj.schemaId)` and does `row ? {...} : null`, but no test exercises `row` resolving to `null`/`undefined` (a genuine cache miss, e.g. schema evicted or never cached). A regression collapsing that ternary (or forgetting the guard) would return `undefined`/throw instead of `null` for a documented no-data case, breaking any caller that pattern-matches on `schema` being null vs an object. | | |
| 20 | pending | medium | `AssetSourceData.js:52 — schema` resolver, missing `resolveInfo` argument (VE-24648) | `parsed = resolveInfo && parseResolveInfo(resolveInfo)` is meant to degrade safely to the full `getSchema` call when `resolveInfo` is absent (`requestedFields` stays `[]`, so neither fast-path condition matches). No test calls `resolver.schema(obj, args, context)` with a valid `schemaId` and no 4th argument — a regression narrowing that guard (e.g. assuming `resolveInfo` is always present) would throw on any non-GraphQL-execution caller instead of falling through to `getSchema`. | | |
| 22 | pending | medium | `Watchlist.js:9 — folders` resolver (VE-27880) | The `folders` resolver (returns the watchlist's parent folder wrapped in an array, or `[]` when none) has **zero** test coverage — not the empty-vs-present shape, nor the `'watchlist'` object-type tag now passed as the 4th/5th args to `dal.folder.getParentFolder`. A regression dropping the array-wrap, mis-ordering the `orgId` argument, or losing the type tag (which folder-lookup pruning now keys on) would go undetected. | | |
| 23 | pending | medium | `Watchlist.js:130 — treeObjectId`, `v2FoldersEnabled` default flip true→false (VE-27880) | The feature-flag default passed to `isEnableFeatureInOrganization('v2FoldersEnabled', …)` flipped from `true` to `false`, but no test exercises either branch of `treeObjectId` for this resolver (virtual-tree-object-id path vs the legacy `dal.folder.getTreeObjectInfoForObject` path). A regression re-flipping the default, or breaking the flag lookup so it silently falls through, would route every unflagged organization through the wrong path with nothing failing. | | |
| 24 | pending | medium | `TemporalDataObject.js:145 — folders`, new `objectType`/`useRequestCache` args (VE-27880) | `folders`'s call to `dal.folder.getParentFoldersForObject` now passes `objectType: 'tdo'` and `useRequestCache: true`, but both existing tests (`tdo not in folder` / `tdo in a folder`) only assert the resolved shape, never the call arguments. A regression dropping the type tag or the request-cache flag (which now folds this lookup with `foldersTreeObjectIds`'s) would go undetected. | | |
| 25 | pending | medium | `TemporalDataObject.js:162 — foldersTreeObjectIds`, zero coverage (VE-27880) | `foldersTreeObjectIds` has **zero** test coverage — not the delegation to `dal.folder.getParentFoldersTreeObjectIds`, the `getOrganizationId(obj, context)` resolution, nor the `objectType: 'tdo'` / `useRequestCache: true` params it now shares with the `folders` resolver above. A regression in any of these would silently return wrong or empty tree-object ids. | | |
| 27 | pending | medium | `util.js:1203 — download()`, `httpCallElapsedMs` histogram (VE-28122) | The new `httpCallElapsedMs` histogram observation (S3-vs-CPU latency split) is emitted on both the success path and the catch/error path, but `util.spec.js` only stubs `metrics.observeHistogram` as a no-op `jest.fn()` and never asserts it was called with `'httpCallElapsedMs'` and the pool label on either path. A regression dropping either observation, or mislabeling the pool, would silently blind this latency metric without failing any test. | | |
| 28 | pending | medium | `cache.js:38,45 — resolverCacheMiss/resolverCacheHit` counters (VE-28122) | `cache.get`'s new `resolverCacheMiss`/`resolverCacheHit` counters, keyed by `objectType`, have no test coverage — `cache.spec.js` exercises the miss/hit code paths directly but never asserts `metrics.incrementCounter` was called with either counter name or the `objectType` label. A regression dropping either counter, or leaking the raw cache key into the label instead of `objectType` (blowing up cardinality), would go undetected. | | |
| 26 | pending | low | `Collection.js:30 — folder`, `'collection'` object-type tag (VE-27880) | The `folder` resolver's test stubs `dal.folder.getParentFolder` with an inline function that ignores its arguments, so the new `'collection'` object-type tag (5th positional arg) passed on every call is never verified. A regression dropping or mis-ordering it would go undetected. | | |
| 1 | implemented | high [security-coverage] | `cache.js — resolver-level cache layer` | A regression in resolver-cache key composition (e.g. failing to scope by orgId/userId) would silently leak cached results across tenants. Verify cache keys carry the documented per-request scope. | cache.spec.js | [#3915](https://github.com/veritone/aiware-core/pull/3915) |
| 10 | implemented | high | `Job.js:38 — getJobStatusFromTaskStatuses (status precedence logic)` | If the cancelled > failed/aborted > all-pending > running/queued/waiting > complete precedence is wrong, jobs show incorrect status in the UI, blocking task monitoring. The existing smoke test calls status once but does not exercise each branch. | Job.spec.js | [#4122](https://github.com/veritone/aiware-core/pull/4122) |
| 2 | implemented | medium | `Subscription.js — field resolvers (frequency / contact / scheduledDay / unsubscribeHash / scheduledTime)` | A regression in field mapping (wrong key path for unsubscribeHash, or null handling for scheduledTime) would silently drop fields from Subscription GraphQL responses. (Note: this resolver handles watchlist-alert subscription entity fields, not WebSocket/pubsub auth.) | Subscription.spec.js | [#3915](https://github.com/veritone/aiware-core/pull/3915) |
| 3 | implemented | medium | Library/Entity family: `Library.js / LibrarySummary.js / LibraryType.js / LibraryEngineModel.js / EntityIdentifier.js / EntityIdentifierType.js / SharedCollection.js` | Library/Entity resolvers; verify documented resolver shapes + org-scoping in field resolvers. | LibrarySummary.spec.js (representative; remaining family files are follow-up coverage gaps) | [#3992](https://github.com/veritone/aiware-core/pull/3992) |
| 4 | implemented | medium | Search/Mention family: `CognitiveSearch.js / CognitiveSearchProfile.js / Mention.js / MentionRating.js / SharedMention.js / CollectionMention.js / SavedSearch.js / Watchlist.js / NielsenTVData.js` | Search/mention resolver family. | MentionRating.spec.js + NielsenTVData.spec.js | [#3992](https://github.com/veritone/aiware-core/pull/3992) |
| 5 | implemented | medium | Engine/Build family: `EngineOverview.js / EngineDependency.js / Build.js / SchemaProperty.js` | Engine resolver family. | SchemaProperty.spec.js (representative) | [#3992](https://github.com/veritone/aiware-core/pull/3992) |
| 6 | implemented | medium | User/Org family: `BasicUserInfo.js / LoginInfo.js / OrganizationInfo.js / RegistrationConfiguration.js / RegistrationConfigurationInfo.js` | User/org resolver family. | BasicUserInfo.spec.js (representative) | [#3992](https://github.com/veritone/aiware-core/pull/3992) |
| 7 | implemented | medium | Audit/Subscription family: `AuditEvent.js / SubscriptionService.js / IngestSlug.js / ApplicationComponent.js` | Audit + subscription resolvers. | AuditEvent.spec.js (representative) | [#3992](https://github.com/veritone/aiware-core/pull/3992) |
| 8 | implemented | medium | Clone/Misc: `CloneRequest.js / CloneData.js / TDOSourceData.js / Widget.js / StructuredData.js / AssetScrollList.js / GraphQLServiceInfo.js` | Misc resolver family. | NielsenTVData.spec.js (covered in row #4; see that row) | [#3992](https://github.com/veritone/aiware-core/pull/3992) |
| 11 | implemented | medium | `Job.js:155 — dagTemplate resolver (dagTemplateId present vs absent)` | A regression in the dagTemplateId/jobId routing would call the wrong DataLoader (dagTemplatesByIds vs dagTemplatesByJobIds) and return null or the wrong template. Verify both paths: when dagTemplateId is set use dagTemplatesByIds.load; when absent use dagTemplatesByJobIds.load. | Job.spec.js | [#4122](https://github.com/veritone/aiware-core/pull/4122) |
| 12 | implemented | medium | `Task.js:92 — executionLocation composite field null guard` | When both taskExecutorId and taskExecutorData are absent, executionLocation must return null (not `{id: '', data: undefined}`). A regression would expose an empty object to callers expecting null, breaking optional-field consumers. | Task.spec.js | [#4122](https://github.com/veritone/aiware-core/pull/4122) |
| 13 | implemented | medium | `IngestSlug.js — dal delegation (source/tdo/asset/tdoId/assetId)` | Each field resolver must pass both the calling context and the slug object to the correct dal method. A regression where args are swapped or the wrong dal method is called would silently return data for the wrong object. | IngestSlug.spec.js | [#4385](https://github.com/veritone/aiware-core/pull/4385) |
| 16 | implemented | medium | `Watchlist.js — searchIndex bool→enum + sourceTypeIds merge` | searchIndex must map true→'mine', false→'global'. sourceTypeIds must merge sourceTypeId into the array without duplicates. Regressions in either break watchlist filtering and source-type reporting. | Watchlist.spec.js | [#4385](https://github.com/veritone/aiware-core/pull/4385) |
| 17 | implemented | medium | `util.js:236 — getSignedUrlOrVirtual` minter-throws fallback | If `virtualAssetMinter.mintStatelessVirtualAssetUri` throws while the `virtualAssetEnabled` flag is on, the `catch` block must log a warning and fall back to `getSignedUrl(uri, bucket, filename)` rather than letting the error propagate. Existing tests cover flag-off, flag-on+our-bucket (success), and flag-on+external-url, but not the minter-failure fallback — a regression removing the try/catch would turn a virtual-asset-minting hiccup into a hard resolver error for every URL-bearing GraphQL field (Application/Engine/Organization/User/etc. all delegate here). | util.spec.js | (pending — framework will open PR) |
| 9 | implemented | low | `index.js — resolver aggregator` | Verify documented re-exports. | index.spec.js | [#3992](https://github.com/veritone/aiware-core/pull/3992) |
| 14 | implemented | low | `GraphQLServiceInfo.js — featureFlags + heartbeatStats passthrough` | featureFlags must return the live config value (not a stale snapshot); heartbeatStats must delegate to monitoring.getHeartbeatStats() each call. A regression breaking either passthrough would expose stale/wrong data to introspection clients. | GraphQLServiceInfo.spec.js | [#4385](https://github.com/veritone/aiware-core/pull/4385) |
| 15 | implemented | low | `CognitiveSearchProfile.js — jsondata identity passthrough` | jsondata must return its argument unchanged. Any transformation would silently corrupt profile data for consumers. | CognitiveSearchProfile.spec.js | [#4385](https://github.com/veritone/aiware-core/pull/4385) |
