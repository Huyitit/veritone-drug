# TODO_TESTS — services/api/core-graphql-server/config

Curated by `worker-psd-aqa`. Each row is one proposed test addition with a clear regression statement.
Anyone (human or this agent) may add rows; only the agent updates the `Implemented` and `PR` columns.
Append-only at the bullet level: once a row exists, don't silently delete it.

## Test framework

| Item                    | Value                                                                                                |
|-------------------------|------------------------------------------------------------------------------------------------------|
| Runner                  | **Jest 29.7.0** (root `package.json`); `jest --coverage` via `pnpm test`                             |
| Test command            | `pnpm --filter core-graphql-server test` (or `pnpm --filter core-graphql-server testfast` no coverage) |
| Test file location      | **Next-to-source** — `config.spec.js` alongside `config.js` (no `__test__/` dir in this service)    |
| Naming convention       | `*.spec.js`                                                                                          |
| Transform               | `transform: {}` — jest.mock NOT auto-hoisted; place `jest.mock(...)` before `require(...)` manually  |
| Mocking pattern         | `jest.mock('module')` before `require()`; or `jest.spyOn` for per-test boundary stubs                |
| Coverage note           | `collectCoverageFrom` in package.json does NOT include `config/**` — tests still run but coverage unreported. |

## Public-surface inventory

### `config.js` — `GraphQLServiceConfig` (exported class)

| Method | Line | Notes |
|--------|------|-------|
| `static async getConfig(provider)` | ~2178 | Calls ConfigLoader.loadConfig + GraphQLServiceFeatureFlags.getFlags; lodash.merges result |
| `getServiceConfigSchema()` | ~2187 | Returns `SERVICE_CONFIG` — pure accessor |
| `getStaticServiceConfigFile()` | ~2191 | Returns `'./config/service.yml'` — constant |
| `getResourceMap()` | ~2195 | Returns Map with `redis` and `es` handlers that mutate a resourceMap |
| `legacyConfigConvert(data, path)` | ~2217 | Converts `s3.buckets` from object-keyed to array form; returns null for other paths |
| `static parseRedisConnectionString(connectionString)` | ~2236 | Regex-parses Redis URI; returns `{user, password, host, port}` or `null` |
| `postProcess(data)` | ~2251 | Conditionally sets `defaultRealTimeCluster` and `aiware.defaultCluster.id`; always sets `log.console.level` |

### `featureFlags.js` — `GraphQLServiceFeatureFlags` (exported class)

| Method | Line | Notes |
|--------|------|-------|
| `static async getFlags(provider)` | ~124 | Wrapper around `ConfigLoader.loadConfig` |
| `getServiceConfigSchema()` | ~128 | Returns `FEATURE_FLAGS` — pure accessor |
| Other methods | — | Trivial stubs (return null/empty Map/data-as-is) |

## Gap rows

| # | Status | Priority | Lens | Subject (file:line — function) | Regression this test would catch | Implemented | PR |
|---|--------|----------|------|-------------------------------|----------------------------------|-------------|----|
| 18 | pending | medium | white-box | `config.js:2446 — GraphQLServiceConfig.sanitizeCspConfig` catch-arm when `sanitizeCspValue` throws | VE-17601 (#4538) added `sanitizeCspConfig`, called from `postProcess`, wrapping `sanitizeCspValue`/`formatSanitizationWarnings` in a try/catch that falls back to the unmodified CSP value plus a warning on any thrown error. The merged commit's own new `describe('server.headers.csp sanitization', ...)` block only exercises the malformed-source, healthy-policy, and no-csp-block paths -- none of them force `sanitizeCspValue` to throw, so the catch arm (the one path that keeps a bad exception from crashing config load at service startup) has zero coverage; a regression removing the try/catch, or one that swallows the error without setting `sanitizationWarnings`, would not be caught by any existing test. Tracked by [VE-26789](https://veritone.atlassian.net/browse/VE-26789) (terminal-Blocked, ghost-redrive cap exhausted, pending human intervention — not re-driven here). |  |  |
| 2 | implemented | high [security-coverage] | black-box | `featureFlags.js — default-OFF flags stay OFF` | A regression defaulting a security-gated flag to `true` would silently expose unfinished code paths; this test asserts all documented default-`false` flags return `false` when loaded without explicit config. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 16 | implemented | high [security-coverage] | black-box | `config.js — virtualAssetEndpoint, virtualAssetStaticEndpoint, virtualAsset.jwt.secret defaults` | A regression changing `virtualAssetEndpoint` (default `'asset'`) or `virtualAssetStaticEndpoint` (default `'asset-static'`, VE-24702) would misroute stateful vs. stateless virtual-asset URIs between services; a regression to `virtualAsset.jwt.secret`'s `default: null` / `env: VIRTUAL_ASSET_JWT_SECRET` binding would silently break the shared-secret fallback to `jwt.secret` used by `routes/virtualAsset.js` across core-graphql/core-admin/core-search. | config.spec.js | (pending — framework will open PR) |
| 20 | implemented | high [security-coverage] | black-box | `config.js:593 — engineTransitSecret default/sensitive flag` | Test would fail if `engineTransitSecret`'s default stopped being `null` or its `sensitive: true` flag were removed, silently exposing the AES-256-GCM engine-secret-transit key (VE-25263) in config dumps/logs and breaking the documented fail-closed behavior when the secret is unset. | config.spec.js | VE-28197 (pending — framework will open PR) |
| 1 | implemented | medium | black-box | `config.js — SERVICE_CONFIG defaults` | A regression in documented default values (DB URLs, redis URL, ES URL, JWT TTL, etc.) would silently change runtime behavior; this test asserts the documented defaults are present in the schema. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 3 | implemented | medium | white-box | `config.js:2236 — parseRedisConnectionString` valid URI with credentials | A regression in the regex would fail to extract user/password/host/port from `redis://user:pass@host:6379`; this test asserts all four fields are parsed correctly. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 4 | implemented | medium | white-box | `config.js:2236 — parseRedisConnectionString` URI without credentials | A regression requiring user:password would break unauthenticated Redis deployments; this test passes `redis://host:6379` and asserts `user` and `password` are both empty strings. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 5 | implemented | medium | white-box | `config.js:2236 — parseRedisConnectionString` returns null for non-Redis string | A regression returning a partial match for a non-Redis URI would silently corrupt the redis config; this test passes a postgres:// string and asserts `null` is returned. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 6 | implemented | medium | white-box | `config.js:2217 — legacyConfigConvert` s3.buckets as array — returned unchanged | A regression wrapping an already-array `s3.buckets` in another array would break bucket lookup in new-format deployments; this test passes `{path: 's3.buckets', data: [{key:'k'}]}` and asserts the array is returned as-is. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 7 | implemented | medium | white-box | `config.js:2217 — legacyConfigConvert` s3.buckets as object — converted to keyed array | A regression skipping the object-to-array conversion would break legacy object-keyed deployments; this test passes a two-key object and asserts each key becomes an element with a `key` field merged in. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 9 | implemented | medium | white-box | `config.js:2251 — postProcess` edgeClusterID set populates defaultRealTimeCluster | A regression dropping the `edgeClusterID → defaultRealTimeCluster` propagation would leave edge deployments without a default real-time cluster; this test passes a non-empty `edgeClusterID` and asserts `defaultRealTimeCluster` equals it. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 10 | implemented | medium | white-box | `config.js:2251 — postProcess` always sets log.console.level from log.level | A regression skipping the `log.console.level` population would break the console-transport configuration at startup; this test asserts `log.console.level` equals the lowercased `log.level` after `postProcess`. | config.spec.js | [#3961](https://github.com/veritone/aiware-core/pull/3961) |
| 15 | implemented | medium | black-box | `config.js — ayrshare config block defaults and sensitive flags` | A regression changing `ayrshare.baseUrl` from `'https://api.ayrshare.com'` would silently point the social-posting adapter at a wrong endpoint; changing `ayrshare.apiKey.sensitive` or `ayrshare.privateKey.sensitive` from `true` would expose credentials in config dumps. Verify the default URL, that `apiKey` and `privateKey` default to `null` with `sensitive: true`, and that `domain` defaults to `null`. | config.spec.js | (pending — framework will open PR) |
| 17 | implemented | medium | black-box | `config.js — s3/oci credential-id + cloud-provider defaults, and elastic.connection HTTP-agent tuning defaults` | A regression changing the new `s3.accessCredentialId`/`s3.cloudProvider`/`oci.accessCredentialId` defaults away from `null` or their `sensitive: true` flag would leak credential IDs in config dumps; a regression to `elastic.connection`'s HTTP-agent tuning defaults (`requestTimeout: 30000`, `maxRetries: 0`, `maxSockets: 25`, `maxFreeSockets: 10`) would silently change outbound-request timeout/pooling behavior for every Elasticsearch call. (Note: corrected during implementation — the timeout/pooling fields live under `elastic.connection`, not `s3`; the `s3`/`oci` credential-id fields are the genuinely new VE-24702 additions.) | config.spec.js | (pending — framework will open PR) |
| 19 | implemented | medium | black-box | `config.js:1046 — queryMonitor.analyze{Enabled,StalenessDays,MaxTables,MaxRunMs} defaults` | VE-25603 (#4480) added an `ANALYZE`-on-`databaseQueryMonitor` sweep gated by `queryMonitor.analyzeEnabled` (default `false`) with `analyzeStalenessDays` (15), `analyzeMaxTables` (10), and `analyzeMaxRunMs` (40000) tuning its scope/budget; none of these four keys appear in `config.spec.js` or anywhere else in the dir. A regression flipping `analyzeEnabled`'s default to `true` would start running `ANALYZE` sweeps against production tables on every deployment without an explicit opt-in; a regression to the other three defaults would silently change sweep frequency, table-count, or time-budget (the code comment notes `analyzeMaxRunMs` shares its NSQ message-timeout budget with the kill sweep — a bad default risks a requeue that repeats the kill sweep too). | config.spec.js | VE-27854 (pending — framework will open PR) |
| 21 | implemented | medium | white-box | `config.js:2420 — getResourceMap 'redis' handler applies a parsed connection string for known resource names` | Test would fail if the `redis` resource-map handler stopped setting `resourceMap.redis`/`resourceMap.coreAdminRedis` from `parseRedisConnectionString(data.URI)` for `resourceName` values present in its internal `configMap`. Zero existing coverage of `getResourceMap()` on `GraphQLServiceConfig` (distinct from the `GraphQLServiceFeatureFlags` variant, which is trivial and already covered). | config.spec.js | VE-28197 (pending — framework will open PR) |
| 22 | implemented | medium | white-box | `config.js:2425 — getResourceMap 'redis' handler ignores unrecognized resource names` | Test would fail if the handler stopped skipping the resourceMap mutation for a `resourceName` not present in `configMap` (e.g. silently writing a config path for an unrelated resource). | config.spec.js | VE-28197 (pending — framework will open PR) |
| 23 | implemented | medium | white-box | `config.js:2434 — getResourceMap 'es' handler sets elastic.connection.host` | Test would fail if the `es` resource-map handler stopped setting `elastic.connection.host` from the resource's `data.URI`, leaving Elasticsearch unreachable after a resource-map refresh. | config.spec.js | VE-28197 (pending — framework will open PR) |
| 24 | implemented | medium | white-box | `config.js:2480 — postProcess sets aiware.defaultCluster.id from edgeClusterID` | Test would fail if `postProcess` stopped populating `aiware.defaultCluster.id` when empty and `edgeClusterID` is set, breaking aiWARE default-cluster resolution on edge deployments (existing coverage — row 9 — only asserts the sibling `defaultRealTimeCluster` field). | config.spec.js | VE-28197 (pending — framework will open PR) |
| 26 | implemented | medium | grey-box | `config.js:2401 — GraphQLServiceConfig.getConfig merges ConfigLoader output with feature flags` | Test would fail if `getConfig` stopped deep-merging (`_.merge`) the `ConfigLoader.loadConfig` result with `GraphQLServiceFeatureFlags.getFlags()` output, or stopped propagating a `ConfigLoader.loadConfig` rejection, silently dropping feature-flag values or masking a config-load failure at service startup. Zero existing coverage of `getConfig` (only the `GraphQLServiceFeatureFlags.getFlags` half of the merge is tested, in `featureFlags.spec.js`). | config.spec.js | VE-28197 (pending — framework will open PR) |
| 8 | implemented | low | white-box | `config.js:2217 — legacyConfigConvert` non-s3-buckets path returns null | A regression applying s3-bucket conversion to other paths would corrupt unrelated config sections; this test passes `path = 'db.core.read'` and asserts `null` is returned. | config.spec.js | [#4138](https://github.com/veritone/aiware-core/pull/4138) |
| 12 | implemented | low | black-box | `config.js — assetSize retry defaults` | A regression changing `assetSize.retryMaxAttempts` from 5 or `retryInitialDelayMs` from 500 would silently alter retry behavior for all asset-size fetches; this test asserts the two documented default values are present in the convict schema. | config.spec.js | [#4138](https://github.com/veritone/aiware-core/pull/4138) |
| 25 | implemented | low | white-box | `config.js:2474 — postProcess leaves cluster fields untouched when edgeClusterID is empty` | Test would fail if `postProcess` started writing `defaultRealTimeCluster`/`aiware.defaultCluster.id` even when `edgeClusterID` is unset, corrupting config for non-edge deployments. | config.spec.js | VE-28197 (pending — framework will open PR) |
| 13 | blocked (config option removed from config.js — master ff5fc764) | medium | black-box | `config.js — server.maxProxyContentSize default` | Test removed: `maxProxyContentSize` was deleted from the `server` config section in master commit ff5fc764 as part of the virtualAsset proxy refactor. No longer testable. |  |  |
| 14 | blocked (config option removed from config.js — master ff5fc764) | medium | black-box | `config.js — folder.maxDepth default` | Test removed: `folder.maxDepth` was deleted from the `folder` config section in master commit ff5fc764. No longer testable. |  |  |
