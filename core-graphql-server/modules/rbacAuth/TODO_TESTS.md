# TODO_TESTS — rbacAuth module

**14 source files, 9 untested.** RBAC AuthZ — security-critical.

## Gaps

| # | Status | Priority | Subject | Regression this test would catch | Implemented | PR |
|---|--------|----------|---------|----------------------------------|-------------|----|
| 1 | implemented | high [security-coverage] | `Mutation.js — RBAC mutation resolvers (grant/revoke)` | A regression in RBAC grant/revoke handlers (e.g. failing to enforce admin-only access, allowing self-grant of elevated permissions) would silently expose privilege-escalation paths. Verify each mutation's authz pre-check + the documented downstream BLL call. | Mutation.spec.js | [#3912](https://github.com/veritone/aiware-core/pull/3912) |
| 2 | implemented | high [security-coverage] | `Query.js — RBAC query resolvers (permission lookups)` | A regression in permission-lookup queries (e.g. dropping the org-scope filter) would silently expose cross-tenant permission data. Verify org-scope on every permission query. | Query.spec.js | [#3912](https://github.com/veritone/aiware-core/pull/3912) |
| 3 | implemented | high [security-coverage] | `PermissionGrant.js — grant model + validation` | A regression in grant-shape validation (e.g. accepting wildcard '*' permission strings without admin scope) would silently bypass the grant restriction. Verify documented field constraints + invalid-input rejection. | PermissionGrant.spec.js | [#3912](https://github.com/veritone/aiware-core/pull/3912) |
| 4 | implemented | medium | `PermissionGrantAudit.js / PermissionAudit.js — audit trail for grants/revokes` | A regression that drops the audit log entry on grant/revoke would silently lose the only forensic trail. Verify every grant/revoke writes a documented audit entry. | PermissionGrantAudit.spec.js | [#3912](https://github.com/veritone/aiware-core/pull/3912) |
| 5 | implemented | medium | `RBACAuthPermissionSet.js / RBACAuthGroup.js — permission set + group models` | Verify documented field shape; the org-scope field is critical for tenant isolation. | RBACAuthPermissionSet.spec.js | [#3912](https://github.com/veritone/aiware-core/pull/3912) |
| 6 | implemented | medium | `ResourceIdentifier.js — resource-identity model` | A regression in resource-identity composition (e.g. accepting cross-tenant resource IDs) would surface as cross-tenant permission grants. Verify documented identity format. | ResourceIdentifier.spec.js | [#3912](https://github.com/veritone/aiware-core/pull/3912) |
| 7 | implemented | low | `index.js — module factory` | Verify documented module shape. | index.spec.js | [#3912](https://github.com/veritone/aiware-core/pull/3912) |
| 8 | implemented | high [security-coverage] | `Query.js:29 — hasPermissions(_, args, context)` (new `{ auditAccessQuery: true }` 4th arg, added VE-25569/VE-19359 OLP audit work) | Query.spec.js has zero tests for this resolver (only `authGroup` and `applicationRoles` are exercised, despite row 2 above claiming "RBAC query resolvers" broadly covered). A regression that drops the `auditAccessQuery: true` flag from this caller-facing resolver's call to `bllRbac.hasPermissions` would silently stop emitting the `ACEQuery` OLP audit event for every external permission check via this GraphQL query, while internal (non-resolver) callers of `hasPermissions` correctly continue to NOT audit-fan-out — verify both that the flag is passed AND that it is passed as the object-literal 4th arg (not merged into `args`, which would otherwise leak into the DAL's whole-args permission-cache key per the resolver's own comment). | Query.spec.js | (pending — framework will open PR) |
