# TODO_TESTS — internalAPI module

**5 source files, all untested.** Internal admin GraphQL surface.

| # | Status | Priority | Subject | Regression this test would catch | Implemented | PR |
|---|--------|----------|---------|----------------------------------|-------------|----|
| 9 | pending | medium | `Query.js:9-16 — untested query resolvers (internalToken, availableRights, scheduledEvent, scheduledEvents)` | Re-reading `Query.spec.js` in full: it only exercises `#internalTokens` (one `describe` block, one test), despite row 4 (implemented, PR #3910) claiming coverage of "internal query resolvers" generally. A regression breaking any of these four passthroughs (wrong dal method, dropped context/args) would go undetected — single-token lookup, rights listing, and scheduled-event read/list would silently no-op or throw with no test catching it. |  |  |
| 10 | pending | low | `InternalToken.js:6-11 — untested field resolvers (approvedDateTime, modifiedDateTime, revokedDateTime, requestorId, approverId)` | Re-reading `InternalToken.spec.js` in full: it only exercises `createdDateTime` (2 tests: present + absent `json`), despite row 1 (implemented, PR #3910) claiming coverage of "field resolvers" (plural — the module exports 6). Each of the other 5 resolvers is an independent one-line `_.get(object, 'json.<field>')` accessor; a typo'd json-path in any one of them would silently drop that field from InternalToken GraphQL responses with no test catching it. |  |  |
| 2 | implemented | high [security-coverage] | `Mutation.js:44 — setAssetStorageTags (isInternalToken guard)` | The token-type guard would be bypassed, allowing non-internal tokens to set asset storage tags (privilege escalation on the storage-tagging operation). | Mutation.spec.js | [#3910](https://github.com/veritone/aiware-core/pull/3910) |
| 3 | implemented | medium | `Mutation.js — internal mutation resolvers` | A regression in resolver delegation would silently break admin mutations (requestInternalToken, approveInternalToken, revokeInternalToken, etc.). Verify each hits its DAL counterpart with correct context + args. | Mutation.spec.js | [#3910](https://github.com/veritone/aiware-core/pull/3910) |
| 4 | implemented | medium | `Query.js — internal query resolvers` | A regression in resolver delegation would surface as broken admin/observability queries. | Query.spec.js | [#3910](https://github.com/veritone/aiware-core/pull/3910) |
| 7 | implemented | medium | `Mutation.js:33 — databaseQueryMonitor passthrough` | VE-25603 (#4480) extended `internalAPI.graphql`'s `databaseQueryMonitor` mutation with `enableAnalyze`/`maxTables`/`stalenessDays` args and new `DatabaseQueryMonitorResult.tables`/`analyzeSummary` fields; the resolver itself (`(root, args, context) => serviceContext.dal.queryMonitor.queryMonitor(context, args)`) has zero coverage in `Mutation.spec.js` — row 3 below claims "each" mutation resolver is verified, but `databaseQueryMonitor` is not among the tests it actually contains. A regression dropping `context` or mutating `args` before the dal call would silently break the query-monitor/analyze admin tooling with no test catching it. | `Mutation.spec.js` ("#databaseQueryMonitor delegates to dal.queryMonitor.queryMonitor with the resolver context and args") | VE-27748 |
| 8 | implemented | medium | `Mutation.js — remaining untested resolvers (approveInternalToken, updateInternalToken, revokeInternalToken, createScheduledEvent, updateScheduledEvent, deleteScheduledEvent)` | Re-reading `Mutation.spec.js` in full: it only exercises `#setAssetStorageTags` and `#requestInternalToken`, despite row 3 (implemented, PR #3910) claiming coverage of "each" resolver's DAL delegation. A regression breaking any of these six passthroughs (wrong dal method, dropped context/args) would go undetected — admin token approval/update/revocation and scheduled-event CRUD would silently no-op or throw. | `Mutation.spec.js` ("remaining admin passthrough resolvers" — 6 new tests) | VE-27748 |
| 1 | implemented | low | `InternalToken.js — field resolvers` | A regression in field extraction (e.g. wrong json key path) would silently drop date/ID fields from InternalToken GraphQL responses. | InternalToken.spec.js | [#3910](https://github.com/veritone/aiware-core/pull/3910) |
| 5 | blocked (needs refactor) | low | `DatabaseQueryMonitorResult.js — query-monitor result model` | Verify documented field shape. | n/a — factory returns empty {} | no exported methods or branches; any assertion would pass trivially |
| 6 | blocked (needs refactor) | low | `index.js — module factory` | Verify documented module shape (resolvers + typeDefs wired correctly). | n/a — refactor needed | requires mocking fs.readFileSync + 3 DAL modules; test would assert only that modules load |
